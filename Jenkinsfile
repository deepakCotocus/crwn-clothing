pipeline {
    agent any
    tools {nodejs "node"}
    stages {
        stage('installation') {
            steps {
                //scripts {
                    sh 'npm install'
                //}
            }
        }
    }
}